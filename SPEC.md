# 소소한시장 운영 도구 스펙

설계 인터뷰(2026-09-01)에서 확정된 요구사항. 용어는 [CONTEXT.md](./CONTEXT.md)를 따르고, 결정의 근거는 [docs/adr](./docs/adr)에 있다.

## 무엇을 만드는가

소소한시장 플리마켓의 **선정 이후** 운영 도구. 담당자가 확정 셀러에게 공지를 작성해 문자로 보내고, 각 플리마켓의 최신 정보를 공개 페이지 하나로 보여준다.

신청 접수는 범위 밖이다([ADR-0002](./docs/adr/0002-신청-접수는-네이버폼에-남겨둔다.md)).

## 확정 요구사항

### 셀러

- 셀러 1명 = 부스 1개 = 연락처 1개로 항상 1:1. "팀"이라는 말은 쓰지 않는다.
- **전화번호가 유일 키.** 같은 번호면 같은 셀러다. 번호가 바뀐 셀러는 담당자가 수동으로 합친다.
- 확정 셀러 명단은 두 경로로 반입한다: **네이버폼 엑셀 업로드**, **이름·전화번호 붙여넣기**.
- 셀러 정보는 **이름과 전화번호 두 가지만** 저장한다.
- 전화번호는 마지막으로 참가한 날로부터 **1년이 지나면 삭제**한다.

### 플리마켓

- 날짜와 장소는 매번 다르다. 고정 일정이 없다.
- **참가비는 플리마켓 단위**다. 같은 플리마켓의 셀러는 모두 같은 금액을 낸다. 받지 않는 플리마켓도 있다.
- 부스 번호를 셀러별로 배정하지 않는다. 자리는 배치도 사진으로만 안내한다.

### 공지

공지는 다섯 종류다: **선정 / 사전 안내 / 리마인드 / 변경 / 배치도**

- 전달 구조: 핵심 정보(날짜·시간·장소·입금)는 **문자 본문**에, 지도·주차·배치도·상세는 **공개 페이지**에 두고 문자에는 링크만 넣는다([ADR-0001](./docs/adr/0001-공지를-문자-전문에서-문자와-링크로-나눈다.md)).
- **개인화되는 값은 셀러 이름 하나뿐이다.** 참가비, 날짜, 장소, 시간은 전부 플리마켓 단위라 전원에게 같은 값이 나간다.
- 공지 5종의 기본 문구를 내장한다. 담당자가 고친 문구는 플리마켓마다 저장되어 다음 플리마켓에서 불러다 쓸 수 있다.

### 공개 페이지

- **플리마켓당 페이지 1개.** 링크는 끝까지 바뀌지 않고, 공지 5종이 그 한 페이지에 누적·갱신된다.
- 로그인 없이 열린다. 따라서 **셀러 개인정보를 일절 표시하지 않는다.** 플리마켓 정보와 배치도만 싣는다.
- 배치도는 사진을 페이지에 올리고 문자로는 링크만 보낸다.

### 문자 발송

- 솔라피 SMS/LMS를 쓴다. 발신번호는 담당자 개인 휴대폰이며 휴대폰 본인인증으로 등록한다(서류·사업자등록 불필요).
- **셀러 1명당 1콜로 순차 발송한다.** 대량 발송 API는 그룹 단위 응답이라 셀러별 실패를 특정할 수 없다. 20~50명 규모에서는 순차 발송이 몇 초면 끝난다.
- 발송 후 **실패한 셀러를 목록으로 보여주고 재발송**할 수 있어야 한다.
- 한글 45자 이하는 SMS, 초과하면 LMS로 자동 전환된다. 작성 화면에서 글자 수와 예상 타입을 보여준다.

### 참가비 입금

- 확정 셀러마다 입금 확인 체크박스를 둔다.
- **미입금 셀러만 골라서** 독촉 공지를 보낼 수 있어야 한다.

### 관리자 화면의 접근 제한

**관리자 화면은 배포하지 않는다.** 담당자 2~3명이 각자 자기 컴퓨터에서 실행하고, 인터넷에 올리는 것은 셀러가 보는 공개 페이지뿐이다.

- 배포본에도 관리자 화면 코드는 함께 올라가므로, `ADMIN_ENABLED` 환경변수가 정확히 `'true'`일 때만 열리는 관문을 둔다. 이 값을 배포 환경에 넣지 않으면 관리자 경로는 404가 된다.
- 관문은 **막히는 방향으로 실패한다.** 값이 없거나 오타가 있으면 닫힌다.
- 솔라피 키는 배포 환경에 올리지 않는다. 문자 발송은 담당자의 로컬에서만 일어난다.
- 문자는 취소가 불가능하므로, **발송 버튼을 누를 때 발송자 이름을 드롭다운에서 고르고** 그 이름이 발송 기록에 남는다.

이 방식의 대가: 담당자 세 명이 각자 저장소를 내려받아 설치해야 하고, 솔라피 키가 컴퓨터 세 대에 흩어진다. 번거로움이 이점보다 커지면 공유 비밀번호 로그인으로 되돌린다 — 관문을 세션 쿠키 검사로 바꾸고 로그인 화면을 하나 더 만들면 된다.

> **이 결정은 처음의 것을 뒤집은 것이다.** 설계 인터뷰에서는 "공유 비밀번호 1개로 로그인"을 골랐다. 그러나 무료 배포 환경에는 플랫폼 차원의 암호 보호가 없고(Vercel의 Password Protection은 Enterprise 또는 월 $150 애드온, 무료 플랜의 Vercel Authentication은 프리뷰 배포만 보호), 담당자가 로그인 기능 자체를 원하지 않았다. 관리자 화면을 아예 배포하지 않는 쪽이 "잠금 없이 배포"보다 안전하다.

### 단톡방

- 플리마켓 당일 실시간 연락 전용이다. 사전 공지는 보내지 않는다. 이 도구의 기능이 아니다.

## Global Constraints

- Node.js 20 이상 (`solapi` v6이 18 이상을 요구)
- `solapi` 패키지는 **서버 전용**이다. API 키가 브라우저 번들에 들어가면 안 된다.
- 전화번호는 저장·발송 전에 항상 `01012345678` 형태로 정규화한다. 하이픈 등 특수문자가 있으면 발송이 실패한다.
- 공개 페이지에는 어떤 경우에도 셀러 이름·전화번호를 렌더링하지 않는다.
- 사용자에게 보이는 문구는 모두 한국어이며, CONTEXT.md의 용어를 그대로 쓴다("팀", "참가자", "어드민" 금지).
- 저장소가 공개이므로 키는 `.env`에만 두고 절대 커밋하지 않는다.

## 범위 밖 (의도적으로 뺀 것)

- 신청 접수 (네이버폼 유지)
- 부스 번호 개별 배정
- 담당자별 계정 분리와 권한 차등
- 카카오 알림톡 (나중에 추가 가능)
- 단톡방 연동

## 비용

고정비 0원(Vercel·Supabase 무료 티어). 문자비만 행사당 1,300~3,300원 수준(SMS 13원, LMS 29원 기준, 셀러 20~50명 × 공지 5회).
